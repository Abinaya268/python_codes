class Solution:
    def compareNM(self, n : int, m : int) -> str:
        if n<m:
            return "lesser"
        elif n>m:
            return "greater"
        else:
            return "equal"
m,n=list(map(int,input().split()))
sol=Solution()
print(sol.compareNM(n,m))